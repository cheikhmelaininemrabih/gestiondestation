{% for user in inactive_users %}
  <tr>
    <td>{{ user.username }}</td>
    <td>
      {% if not user.is_active %}
        <a href="{% url 'admin_activate_user' user.id %}">Activate</a>
      {% endif %}
    </td>
  </tr>
{% endfor %}
